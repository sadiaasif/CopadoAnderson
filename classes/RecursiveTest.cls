@isTest
private class RecursiveTest {

    static testMethod void getRecursiveTest(){
         Test.startTest();
          Recursive.firstRun = true;
          Recursive.isUpadteOpp = false;
          Test.stopTest();  
    }
}